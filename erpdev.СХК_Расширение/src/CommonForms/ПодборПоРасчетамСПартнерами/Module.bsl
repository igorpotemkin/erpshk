&После("РассчитатьСуммуПлатежей")
&НаКлиенте
Процедура РассчитатьСуммуПлатежей_Расш()
	Дельта = СуммаДокумента - Суммаплатежей;
КонецПроцедуры

&После("УстановитьВидимость")
&НаСервере
Процедура УстановитьВидимость_РАсш()
	Если СуммаДокумента = 0 Тогда
		Элементы.Дельта.Видимость = Ложь;
	КонецЕсли;
КонецПроцедуры

